@model List<JuvenileSportsManagementApp.Models.AthleteVM>


@{
    ViewBag.Title = "Athletics Management Portal - Results By Athlete";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>
    <h2>Athletes</h2>

    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>
                    Athlete
                </th>
                <th>

                </th>
            </tr>
        </thead>

        <tbody>
            @foreach (var m in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(model => m.AthleteName)
                    </td>
                    <td>
                        @Html.ActionLink("View Results", "AthleteResults", "Results", new { ID = m.AthleteID }, htmlAttributes: new { id = "registerLink", @class = "btn btn-primary" })
                    </td>
                </tr>
            }
        </tbody>
    </table>

</body>
</html>
